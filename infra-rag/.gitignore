# Terraform state — nunca versionar
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Variáveis reais — nunca versionar
terraform.tfvars

# Build artifacts
.build/*.zip

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json
