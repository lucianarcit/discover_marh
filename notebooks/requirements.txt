# Dependências do Notebook de Validação e Demonstração — Fase 3 RAG
# Modo OFFLINE_EVIDENCE: sem boto3, sem credenciais AWS
#
# Instalar:
#   cd C:\proj\discover_alelo
#   .\.venv\Scripts\Activate.ps1
#   python -m pip install -r .\notebooks\requirements.txt

ipykernel>=6.29,<7
jupyterlab>=4.2,<5
nbformat>=5.10,<6
nbconvert>=7.16,<8
pandas>=2.2,<3
matplotlib>=3.8,<4
pytest>=8,<9

# Nota: boto3 NÃO é necessário para EXECUTION_MODE="OFFLINE_EVIDENCE".
# Adicionar apenas quando LIVE_HML for implementado no Passo 9.
